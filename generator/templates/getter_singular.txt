func (this *<this.type>) Get<field.name.upper.singular>() <field.type> {
    if len(this.<field.name.lower.plural>) == 0 {
        return <field.type>{}
    }

    return this.<field.name.lower.plural>[0]
}

